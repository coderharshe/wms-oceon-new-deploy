// The same screen as Admin's — /api/admin/reports has always accepted a
// manager, only the page was missing. One component, so the two portals can't
// drift apart.
export { default } from "@/app/admin/reports/page";
